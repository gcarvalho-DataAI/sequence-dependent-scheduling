# Third-Party Dependencies

This project expects OR-Tools to live in:

```
third_party/or-tools
```

Clone it like this:

```bash
git clone https://github.com/google/or-tools.git third_party/or-tools
```

Then build it following the instructions in the main `README.md`.
